{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Extension",
      "type": "extensionHost",
      "request": "launch",
      "args": ["--extensionDevelopmentPath=${workspaceFolder:vscode-agent-tracing}"],
      "outFiles": ["${workspaceFolder:vscode-agent-tracing}/dist/**/*.js"],
      "preLaunchTask": "watch"
    }
  ]
}
